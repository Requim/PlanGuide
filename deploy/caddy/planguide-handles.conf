# Put this handle before the existing catch-all reverse_proxy in each site block.
# The PlanGuide nginx container must share a Docker network with the Caddy container.
handle /plan* {
    reverse_proxy planguide-nginx:8010
}
